{% extends 'layout_page.twig' %}

{% set page = 'commits' %}
{% set titre = repo | replace({'codes/':'', '\.git':'', '_':' ', '-':' '}) | capitalize %}

{% block title %}{{ titre }} - Archéo Lex{% endblock %}

{% block content %}
<h1>{{ repo | replace({'codes/':'', '\.git':'', '_':' ', '-':' '}) | capitalize }}</h1>
<hr />

    <div class="panel panel-default">
        <div class="panel-body">
            <p>Ci-dessous sont présentées les modifications introduites à la date donnée. L’ancien texte est en rouge, le texte introduit à cette date est en vert.</p>
            <p>Si les lignes sont trop longues, un ascenseur vertical est disponible en fin de page.</p>
        </div>
    </div>

    {% for diff in commit.diffs %}
    <div class="source-view">
        <div class="source-header">
            <div class="btn-group pull-right">
                <a href="{{ path('version', {repo: repo | replace({'.git': ''}), version: commit.date|date('Y-m-d')}) }}"  class="btn btn-default btn-sm"><span class="fa fa-file"></span> Voir cette version</a>
                <a href="{{ path('blameversion', {repo: repo|replace({'.git': ''}), version: commit.date|date('Y-m-d')}) }}" class="btn btn-default btn-sm"><span class="fa fa-bullhorn"></span> Texte annoté</a>
                <a href="{{ path('summaryrepo', {repo: (repo | replace({'.git': ''}))}) }}"  class="btn btn-default btn-sm"><span class="fa fa-list"></span> Historique</a>
            </div>
            <h4>{{ commit.message }} <small>(version {{ commit.shortHash }})</small></h4>
        </div>
        <div class="source-diff">
        <table>
        {% for line in diff.getLines %}
            <tr>
                <td class="lineNo">
                    {% if line.getType != 'chunk' %}
                        <a name="L{{ loop.index }}R{{ line.getNumOld }}"></a>
                        <a href="#L{{ loop.index }}R{{ line.getNumOld }}">
                    {% endif %}
                    {{ line.getNumOld }}
                    {% if line.getType != 'chunk' %}
                        </a>
                    {% endif %}
                </td>
                <td class="lineNo">
                    {% if line.getType != 'chunk' %}
                        <a name="L{{ loop.index }}L{{ line.getNumNew }}"></a>
                        <a href="#L{{ loop.index }}L{{ line.getNumNew }}">
                    {% endif %}
                    {{ line.getNumNew }}
                    {% if line.getType != 'chunk' %}
                        </a>
                    {% endif %}
                </td>
                <td style="width: 100%">
                    <pre{% if line.getType %} class="{{ line.getType }}"{% endif %}>{{ line.getLine }}</pre>
                </td>
            </tr>
        {% endfor %}
        </table>
        </div>
    </div>
    {% endfor %}

    <hr />
{% endblock %}
