{% extends 'layout_page.twig' %}

{% set page = 'stats' %}

{% block title %}GitList{% endblock %}

{% block content %}
    {% include 'breadcrumb.twig' with {breadcrumbs: [{dir: 'Statistiques', path:''}]} %}

	<div class="table-border hidden-xs">
        <table class="table stats">
            <thead>
                <tr>
                    <th width="30%"><span class="fa fa-file-text"></span> Extensions de fichiers ({{ stats.extensions|length }})</th>
                    <th width="40%"><span class="fa fa-users"></span> Auteurs ({{ authors|length }})</th>
                    <th width="30%"><span class="fa fa-star"></span> Autre</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>
                        <ul>
                        {% for ext, amount in stats.extensions %}
                            <li><strong>{{ ext }}</strong> : {{ amount }} fichiers</li>
                        {% endfor %}
                        </ul>
                    </td>
                    <td>
                        <ul>
                        {% for author in authors %}
                            <li><strong><a href="mailto:{{ author.email }}">{{ author.name }}</a></strong> : {{ author.commits }} versions</li>
                        {% endfor %}
                        </ul>
                    </td>
                    <td>
                        <p>
                            <strong>Total en fichiers :</strong> {{ stats.files }}
                        </p>
     
                        <p>
                            <strong>Total en octets :</strong> {{ stats.size }} octets ({{ ((stats.size / 1024) / 1024) | number_format }} Mio)
                        </p>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
    <div class="panel panel-default visible-xs">
        <div class="panel-heading bold"><span class="fa fa-file-text"></span> Extensions de fichiers ({{ stats.extensions|length }})</div>
        <div class="panel-body">
            <ul>
            {% for ext, amount in stats.extensions %}
                <li><strong>{{ ext }}</strong> : {{ amount }} fichiers</li>
            {% endfor %}
            </ul>
        </div>
    </div>
    <div class="panel panel-default visible-xs">
        <div class="panel-heading bold"><span class="fa fa-users"></span> Auteurs ({{ authors|length }})</div>
        <div class="panel-body">
            <ul>
            {% for author in authors %}
                <li><strong><a href="mailto:{{ author.email }}">{{ author.name }}</a></strong> : {{ author.commits }} versions</li>
            {% endfor %}
            </ul>
        </div>
    </div>
    <div class="panel panel-default visible-xs">
        <div class="panel-heading bold"><span class="fa fa-star"></span> Autre</div>
        <div class="panel-body">
            <p>
                <strong>Total en fichiers :</strong> {{ stats.files }}
            </p>
     	    
            <p>
                <strong>Total en octets :</strong> {{ stats.size }} octets ({{ ((stats.size / 1024) / 1024) | number_format }} Mio)
            </p>
        </div>
    </div>

    <hr />
{% endblock %}
